diff --git a/src/Ryujinx.Graphics.Vulkan/Window.cs b/src/Ryujinx.Graphics.Vulkan/Window.cs
index eb8d433..f04d18f 100644
--- a/src/Ryujinx.Graphics.Vulkan/Window.cs
+++ b/src/Ryujinx.Graphics.Vulkan/Window.cs
@@ -309,6 +309,8 @@ namespace Ryujinx.Graphics.Vulkan
             return new Extent2D(width, height);
         }
 
+        private static readonly bool _latencyProbe = Environment.GetEnvironmentVariable("NEL3AB_LATENCY_PROBE") == "1";
+
         public unsafe override void Present(ITexture texture, ImageCrop crop, Action swapBuffersCallback)
         {
             _gd.PipelineInternal.AutoFlush.Present();
@@ -484,6 +486,13 @@ namespace Ryujinx.Graphics.Vulkan
             {
                 _gd.SwapchainApi.QueuePresent(_gd.Queue, in presentInfo);
             }
+
+            // nel3ab, sonde de latence (jamais dans la salle) : l'image vient
+            // d'être remise au compositeur, en horloge monotone.
+            if (_latencyProbe)
+            {
+                Console.Error.WriteLine($"NEL3AB_PRESENT t_ns={System.Diagnostics.Stopwatch.GetTimestamp()}");
+            }
         }
 
         public override void SetAntiAliasing(AntiAliasing effect)
diff --git a/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/BufferQueueProducer.cs b/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/BufferQueueProducer.cs
index 9afb26c..4742909 100644
--- a/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/BufferQueueProducer.cs
+++ b/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/BufferQueueProducer.cs
@@ -11,6 +11,12 @@ namespace Ryujinx.HLE.HOS.Services.SurfaceFlinger
 {
     class BufferQueueProducer : IGraphicBufferProducer
     {
+        // nel3ab, sonde de latence (jamais dans la salle) : horodatages en
+        // horloge monotone (Stopwatch lit CLOCK_MONOTONIC sous Linux). Le seul
+        // verrou que ces lignes prennent est celui de la console, qui n'appelle
+        // jamais le code de l'émulateur : aucun ordre de verrous ne s'inverse.
+        private static readonly bool _latencyProbe = Environment.GetEnvironmentVariable("NEL3AB_LATENCY_PROBE") == "1";
+
         public BufferQueueCore Core { get; }
 
         private readonly ITickSource _tickSource;
@@ -406,6 +412,12 @@ namespace Ryujinx.HLE.HOS.Services.SurfaceFlinger
                 item.IsAutoTimestamp = input.IsAutoTimestamp != 0;
                 item.SwapInterval = input.SwapInterval;
                 item.FrameNumber = Core.FrameCounter;
+
+                // Le jeu remet son image N à l'émulateur.
+                if (_latencyProbe)
+                {
+                    Console.Error.WriteLine($"NEL3AB_QUEUE t_ns={System.Diagnostics.Stopwatch.GetTimestamp()} frame={item.FrameNumber}");
+                }
                 item.Slot = slot;
                 item.Fence = input.Fence;
                 item.IsDroppable = Core.DequeueBufferCannotBlock || input.Async != 0;
diff --git a/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/SurfaceFlinger.cs b/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/SurfaceFlinger.cs
index 9717781..5ce3a8a 100644
--- a/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/SurfaceFlinger.cs
+++ b/src/Ryujinx.HLE/HOS/Services/SurfaceFlinger/SurfaceFlinger.cs
@@ -15,6 +15,12 @@ namespace Ryujinx.HLE.HOS.Services.SurfaceFlinger
 {
     class SurfaceFlinger : IConsumerListener, IDisposable
     {
+        // nel3ab, sonde de latence (jamais dans la salle) : horodatages en
+        // horloge monotone (Stopwatch lit CLOCK_MONOTONIC sous Linux). Le seul
+        // verrou que ces lignes prennent est celui de la console, qui n'appelle
+        // jamais le code de l'émulateur : aucun ordre de verrous ne s'inverse.
+        private static readonly bool _latencyProbe = Environment.GetEnvironmentVariable("NEL3AB_LATENCY_PROBE") == "1";
+
         private readonly Switch _device;
 
         private readonly Dictionary<long, Layer> _layers;
@@ -372,6 +378,12 @@ namespace Ryujinx.HLE.HOS.Services.SurfaceFlinger
 
                 if (acquireStatus == Status.Success)
                 {
+                    // Le compositeur émulé prend l'image N, à sa synchronisation verticale.
+                    if (_latencyProbe)
+                    {
+                        Console.Error.WriteLine($"NEL3AB_ACQUIRE t_ns={Stopwatch.GetTimestamp()} frame={item.FrameNumber}");
+                    }
+
                     if (_device.VSyncMode == VSyncMode.Unbounded)
                     {
                         if (_swapInterval != 0)
@@ -505,7 +517,19 @@ namespace Ryujinx.HLE.HOS.Services.SurfaceFlinger
 
         private void AcquireBuffer(TextureCallbackInformation information)
         {
+            // Le fil GPU commence à présenter l'image N, puis attend la barrière
+            // qui dit que le jeu a fini de la dessiner.
+            if (_latencyProbe)
+            {
+                Console.Error.WriteLine($"NEL3AB_RENDER t_ns={Stopwatch.GetTimestamp()} frame={information.Item.FrameNumber}");
+            }
+
             information.Item.Fence.WaitForever(_device.Gpu);
+
+            if (_latencyProbe)
+            {
+                Console.Error.WriteLine($"NEL3AB_READY t_ns={Stopwatch.GetTimestamp()} frame={information.Item.FrameNumber}");
+            }
         }
 
         public static Format ConvertColorFormat(ColorFormat colorFormat)
diff --git a/src/Ryujinx.Input/HLE/NpadManager.cs b/src/Ryujinx.Input/HLE/NpadManager.cs
index fcc527c..7abef12 100644
--- a/src/Ryujinx.Input/HLE/NpadManager.cs
+++ b/src/Ryujinx.Input/HLE/NpadManager.cs
@@ -32,6 +32,11 @@ namespace Ryujinx.Input.HLE
 
         private readonly IGamepadDriver _keyboardDriver;
         private readonly IGamepadDriver _gamepadDriver;
+        // nel3ab, sonde de latence (jamais dans la salle) : l'instant où l'état
+        // du joueur 1 change dans la mémoire HID que le jeu lit, en horloge
+        // monotone. Stopwatch lit CLOCK_MONOTONIC sous Linux.
+        private static readonly bool _latencyProbe = Environment.GetEnvironmentVariable("NEL3AB_LATENCY_PROBE") == "1";
+        private ulong _probeButtons;
         private readonly IGamepadDriver _mouseDriver;
         private bool _isDisposed;
 
@@ -412,6 +417,12 @@ namespace Ryujinx.Input.HLE
 
                         inputState.Buttons |= _device.Hid.UpdateStickButtons(inputState.LStick, inputState.RStick);
 
+                        if (_latencyProbe && playerIndex == PlayerIndex.Player1 && (ulong)inputState.Buttons != _probeButtons)
+                        {
+                            _probeButtons = (ulong)inputState.Buttons;
+                            Console.Error.WriteLine($"NEL3AB_HID t_ns={Stopwatch.GetTimestamp()} f={Stopwatch.Frequency} buttons={_probeButtons:x}");
+                        }
+
                         isJoyconPair = inputConfig.ControllerType == ControllerType.JoyconPair;
 
                         SixAxisInput altMotionState = isJoyconPair ? controller.GetHLEMotionState(true) : default;
